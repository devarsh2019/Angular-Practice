import { PipeDataListPipe } from './pipe-data-list-pipe';

describe('PipeDataListPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeDataListPipe();
    expect(pipe).toBeTruthy();
  });
});
